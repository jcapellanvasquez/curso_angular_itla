import { NotificacionDirective } from './notificacion.directive';

describe('NotificacionDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificacionDirective();
    expect(directive).toBeTruthy();
  });
});
